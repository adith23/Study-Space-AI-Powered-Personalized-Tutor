# ============================================================
# Production-only requirements (slim — saves ~1 GB vs full)
#
# Removed: sentence-transformers, faiss-cpu, numpy, pandas,
#          scipy, openai, anthropic, python-docx, python-pptx,
#          openpyxl, manim, pytesseract, flower, redis, celery,
#          python-magic, and all dev/test tools.
# ============================================================

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 breaks with bcrypt 4.1+ (72-byte strictness)
bcrypt>=4.0.1,<4.1

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9

# Validation & Settings
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# HTTP Client
httpx>=0.25.0
aiohttp>=3.9.0

# AI/ML (lightweight — no local embedding models)
tiktoken>=0.5.0
langchain>=1.0.0
langchain-classic>=1.0.0
langchain-community>=0.4.0
langchain-text-splitters>=1.0.0
langchain-google-genai>=0.0.3

# Document Processing
docling>=1.0.0
PyPDF2>=3.0.0
Pillow>=10.0.0

# Vector DB
pinecone-client>=3.0.0

# Google AI
google-genai>=1.0.0

# AWS SDK (SQS, SSM integration)
boto3>=1.34.0

# Monitoring
prometheus-client>=0.19.0

# Utilities
python-dotenv>=1.0.0
